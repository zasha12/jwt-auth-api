# jwt-auth-api
 JWT Authentication API built with Spring Boot + Spring Security — register &amp; login with BCrypt password hashing, JJWT token generation/validation, role-based protected endpoints, and MySQL-backed users. Stateless, session-free auth.
